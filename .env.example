# Optional GitHub token for the archive page server fetch.
# Create a fine-grained personal access token on GitHub with:
# Repository permissions -> Metadata: Read-only
# See README.md for the full setup steps.
GITHUB_TOKEN=
